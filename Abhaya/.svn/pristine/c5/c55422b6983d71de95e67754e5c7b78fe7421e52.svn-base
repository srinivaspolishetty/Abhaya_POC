import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VehicleauthdriversComponent } from './vehicleauthdrivers.component';

describe('VehicleauthdriversComponent', () => {
  let component: VehicleauthdriversComponent;
  let fixture: ComponentFixture<VehicleauthdriversComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VehicleauthdriversComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VehicleauthdriversComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
