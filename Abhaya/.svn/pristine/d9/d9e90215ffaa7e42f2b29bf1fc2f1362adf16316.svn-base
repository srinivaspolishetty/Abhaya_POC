import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DevicecommunitationComponent } from './devicecommunitation.component';

describe('DevicecommunitationComponent', () => {
  let component: DevicecommunitationComponent;
  let fixture: ComponentFixture<DevicecommunitationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DevicecommunitationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DevicecommunitationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
