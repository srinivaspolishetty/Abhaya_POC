export class UserLevel {

    public id: Number;
    public name: string;
    public code: string;

    constructor(id: Number, name: string, code: string) {
        this.id = id;
        this.name = name;
        this.code = code;
    }
}
